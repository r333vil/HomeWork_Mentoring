// Znajdź pierwszy element w tablicy [3, 6, 8, 9, 15], który jest większy od 7
// (metoda find).

const array = [3, 6, 8, 9, 15];

const first_element = array.find(curr => curr > 7)

console.log("Pierwszy element wiekszy od 7 to: "+first_element);

